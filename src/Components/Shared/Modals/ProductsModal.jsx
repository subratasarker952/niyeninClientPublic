
const ProductsModal = ({ modalProducts }) => {

    return (
        <div>
            <dialog id="productsModal" className="modal">
             
            </dialog>
        </div>
    );
};

export default ProductsModal;